[package]
name = "workshop-protocol"
version = "0.0.0"
publish = false
edition.workspace = true
license.workspace = true
repository.workspace = true

description = "Workshop wire protocol: the /ws frames and the /agents/ws input-wait frames, typed with zero I/O"

[dependencies]
serde.workspace = true
serde_json.workspace = true
workspace-hack.workspace = true

[lints]
workspace = true
