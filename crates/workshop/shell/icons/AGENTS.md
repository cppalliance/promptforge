# Workshop icons

- Keep the Gateway configuration UI and Workshop UI icon copies synchronized with the corresponding master icons in this directory.
- `installer-header.png`, `installer-header.bmp`, `installer-sidebar.png`, `installer-sidebar.bmp`, and `dmg-background.png` are hand-crafted installer assets. Do not regenerate, resize, overwrite, or modify them as part of ordinary icon generation.
