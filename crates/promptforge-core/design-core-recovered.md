# Historical recovered `promptforge-core` design

> This recovery artifact describes the runtime before the section-lifecycle migration. It is retained only as provenance and is not a grammar, API, or behavior reference. The authoritative current design is `design-core.md`.
