[package]
name = "promptforge-store"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

description = "PromptForge run-scoped virtual files: the Store facade over the shared VFS, built by Store::new(&access)"
readme = "README.md"
keywords = ["prompt", "llm", "virtual-filesystem", "storage"]
categories = ["data-structures", "rust-patterns"]
documentation = "https://cppalliance.github.io/promptforge/"

[dependencies]
promptforge-vfs.workspace = true
thiserror.workspace = true
workspace-hack.workspace = true

[dev-dependencies]
# Surface items' doc examples name `promptforge::` paths, the only paths
# hosts see; only this crate's doctest run executes them.
promptforge.workspace = true

[lints]
workspace = true
