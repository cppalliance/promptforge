# Exact source and public-root counts for the safe Whisper backend.
# Physical lines include comments and blanks. Every recorded ceiling equals
# the measured file size, so any size change updates this manifest explicitly.

public_root_count = 2

[modules]
"config.rs" = 32
"lib.rs" = 8
"model.rs" = 293
"prompt.rs" = 237
