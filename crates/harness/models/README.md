# harness-models

The harness's model client: the HTTP transport that performs the engine's `Chat` effects against the gateway a client has bound, streaming deltas back to the session. Private to the harness family; clients reach it through `harness-api`.
