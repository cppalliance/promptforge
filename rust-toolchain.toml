# Pin to the MSRV channel (rust-version in Cargo.toml): a developer with an
# older default toolchain gets the right one through rustup instead of a
# compile error. CI overrides this with RUSTUP_TOOLCHAIN where it means to
# test a different channel.
[toolchain]
channel = "1.89"
