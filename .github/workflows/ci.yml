name: CI

on:
  push:
    branches: [master, main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install UI dependencies
        working-directory: crates/promptforge-workshop-server/ui
        run: npm ci

      - name: Install config UI dependencies
        working-directory: crates/promptforge-gateway-config-ui/ui
        run: npm ci

      - name: Format
        run: cargo fmt --all --check

      # promptforge-gateway and promptforge-transcribe are excluded from the
      # blanket commands: --all-features would enable workshop-cuda and the
      # transcription engine's cuda feature, which need a CUDA Toolkit these
      # runners do not have. Their non-CUDA surfaces are exercised by the
      # dedicated steps below.
      - name: Clippy
        run: cargo clippy --workspace --exclude promptforge-workshop --exclude promptforge-workshop-server --exclude promptforge-desktop-shell --exclude promptforge-gateway --exclude promptforge-transcribe --all-targets --all-features -- -D warnings

      - name: Test
        run: cargo test --locked --workspace --exclude promptforge-workshop --exclude promptforge-workshop-server --exclude promptforge-desktop-shell --exclude promptforge-gateway --exclude promptforge-transcribe --all-features

      - name: Doctests
        run: cargo test --workspace --exclude promptforge-workshop --exclude promptforge-workshop-server --exclude promptforge-desktop-shell --exclude promptforge-gateway --exclude promptforge-transcribe --doc

      - name: Docs
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --workspace --no-deps --all-features --exclude promptforge-workshop --exclude promptforge-workshop-server --exclude promptforge-desktop-shell --exclude promptforge-gateway --exclude promptforge-transcribe

      # The transcription engine without its cuda feature: whisper then builds
      # its CPU backend, which needs no toolkit.
      - name: Clippy (transcribe)
        run: cargo clippy -p promptforge-transcribe --all-targets -- -D warnings

      - name: Test (transcribe)
        run: cargo test --locked -p promptforge-transcribe

      - name: Docs (transcribe)
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --no-deps -p promptforge-transcribe

      # The workshop feature hosts the ws-server UI; its build script bundles
      # the UI with esbuild, which needs the node_modules installed above.
      - name: Clippy (gateway)
        run: cargo clippy -p promptforge-gateway --all-targets --features workshop -- -D warnings

      - name: Test (gateway)
        run: cargo test --locked -p promptforge-gateway --features workshop

  check-workshop:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install UI dependencies
        working-directory: crates/promptforge-workshop-server/ui
        run: npm ci

      # promptforge-workshop pins the gateway's config-ui feature, so its
      # build script needs the config UI's node_modules even with
      # --no-default-features.
      - name: Install config UI dependencies
        working-directory: crates/promptforge-gateway-config-ui/ui
        run: npm ci

      # --no-default-features: the desktop crate defaults to the cuda
      # feature, and windows-latest has no CUDA toolkit; CI builds the
      # CPU-only whisper path instead.
      - name: Clippy (workshop)
        run: cargo clippy -p promptforge-workshop -p promptforge-workshop-server -p promptforge-desktop-shell --all-targets --no-default-features -- -D warnings

      - name: Test (workshop)
        run: cargo test --locked -p promptforge-workshop -p promptforge-workshop-server -p promptforge-desktop-shell --no-default-features

  # The UI build is its own job: it typechecks, tests, and packages the
  # versioned ui/dist artifact that release builds of promptforge-workshop-server
  # verify and embed (the debug-profile cargo jobs above still drive the UI
  # build in place through the crate's build script, hence their npm ci).
  ui:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install UI dependencies
        working-directory: crates/promptforge-workshop-server/ui
        run: npm ci

      - name: Typecheck
        working-directory: crates/promptforge-workshop-server/ui
        run: npm run typecheck

      - name: Build
        working-directory: crates/promptforge-workshop-server/ui
        run: npm run build

      - name: Test
        working-directory: crates/promptforge-workshop-server/ui
        run: npm test

      - name: Package the UI artifact
        working-directory: crates/promptforge-workshop-server/ui
        run: npm run package

      - name: Upload the UI artifact
        uses: actions/upload-artifact@v4
        with:
          name: promptforge-workshop-ui-dist
          path: crates/promptforge-workshop-server/ui/dist/

      - name: Install config UI dependencies
        working-directory: crates/promptforge-gateway-config-ui/ui
        run: npm ci

      - name: Typecheck config UI
        working-directory: crates/promptforge-gateway-config-ui/ui
        run: npm run typecheck

      - name: Build config UI
        working-directory: crates/promptforge-gateway-config-ui/ui
        run: npm run build

      - name: Test config UI
        working-directory: crates/promptforge-gateway-config-ui/ui
        run: npm test

      - name: Package the config UI artifact
        working-directory: crates/promptforge-gateway-config-ui/ui
        run: npm run package

      - name: Upload the config UI artifact
        uses: actions/upload-artifact@v4
        with:
          name: promptforge-gateway-config-ui-dist
          path: crates/promptforge-gateway-config-ui/ui/dist/

  msrv:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@1.89.0
        with:
          components: rustfmt

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install UI dependencies
        working-directory: crates/promptforge-workshop-server/ui
        run: npm ci

      - name: Install config UI dependencies
        working-directory: crates/promptforge-gateway-config-ui/ui
        run: npm ci

      - name: Build and test on MSRV
        run: |
          cargo build --locked --workspace --exclude promptforge-workshop --exclude promptforge-workshop-server --exclude promptforge-desktop-shell --exclude promptforge-gateway --exclude promptforge-transcribe --all-features
          cargo test --locked --workspace --exclude promptforge-workshop --exclude promptforge-workshop-server --exclude promptforge-desktop-shell --exclude promptforge-gateway --exclude promptforge-transcribe --all-features

      - name: Build and test gateway on MSRV
        run: |
          cargo build --locked -p promptforge-gateway --features workshop
          cargo test --locked -p promptforge-gateway --features workshop

      - name: Build and test transcribe on MSRV
        run: |
          cargo build --locked -p promptforge-transcribe
          cargo test --locked -p promptforge-transcribe

  supply-chain:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-deny and cargo-audit
        run: cargo install cargo-deny cargo-audit --locked

      - name: cargo deny
        run: cargo deny check

      - name: cargo audit
        run: cargo audit
